@namespace NeedBodies.Components
@using NeedBodies.Auth

<span>Game stuff</span>

@if (Game != null)
{
    <span>@Game.DisplayName</span>
}

@code {
    [Parameter] public Data.Game? Game { get; set; } = null;
}